import { serverNodeAppOneFeatureSomeLibTwo } from './server-node-app-one-feature-some-lib-two';

describe('serverNodeAppOneFeatureSomeLibTwo', () => {
  it('should work', () => {
    expect(serverNodeAppOneFeatureSomeLibTwo()).toEqual(
      'server-node-app-one-feature-some-lib-two'
    );
  });
});
