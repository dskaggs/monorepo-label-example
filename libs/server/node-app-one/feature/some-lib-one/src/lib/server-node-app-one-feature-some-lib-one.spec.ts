import { serverNodeAppOneFeatureSomeLibOne } from './server-node-app-one-feature-some-lib-one';

describe('serverNodeAppOneFeatureSomeLibOne', () => {
  it('should work', () => {
    expect(serverNodeAppOneFeatureSomeLibOne()).toEqual(
      'server-node-app-one-feature-some-lib-one'
    );
  });
});
